---
name: prd-document-writer
description: 按内置的团队中文产品需求文档模板，撰写、补全或优化可直接复制到语雀/文档系统的 PRD。适用于新增功能、交互改版、灰度发布、权益/额度、后端数据逻辑、前端与后端埋点、后台维护需求及验收标准等产品需求；默认无需用户重复上传 PRD 模板，只有用户明确提供新版模板时才按新版覆盖。
---

# 中文 PRD 撰写规范

## 目标

将零散的业务描述、原型说明和技术约束整理成结构完整、可评审、可研发落地的中文 PRD。默认输出 Markdown 表格与编号文本，保证复制到语雀后结构清晰；除非用户明确要求，否则不擅自改成其他文档风格。

## 工作流程

1. 默认加载本技能内置的团队 PRD 模板，不要求用户重复上传模板。只有用户明确说“按这个新模板/新截图/新文档”时，才提取新版的章节顺序、表头、编号层级和内容排布，并以新版覆盖同名规则。
2. 归纳需求范围：背景、目标、角色、入口、页面、状态、交互、数据、权限、异常、埋点和验收。
3. 标记不确定信息为“待确认”，不得凭空编造接口、排期、指标或会议结论。
4. 按内置标准章节生成初稿，再逐项检查功能、交互和数据逻辑是否闭环。
5. 输出“可直接复制”的完整内容；如用户只要求某一章节，只输出该章节并保持原章节编号。

## 标准章节与排布

默认使用以下内置模板；用户明确提供新版模板时，才按用户新版模板调整：

1. 版本记录：`日期 | 版本 | 内容 | 修订人`
2. `一、需求背景`：需求方、产品/任务链接、业务问题、使用场景、业务诉求、上线排期、价值收益。信息使用表格，场景和收益使用编号列表。
3. `二、需求调研`：现有产品线能力、相关能力/竞品、现状与目标差异、业务规则确认。
4. `三、产品方案概述`：预期实现方案、集成能力表、功能清单与优先级表（ID、功能、覆盖范围、优先级）。
5. `四、产品方案`：按功能模块拆解。每个模块包含“系统/模块、产品原型、需求描述”或等价结构，并详细写入口、页面元素、交互逻辑、数据逻辑、异常处理和状态。
6. `五、非功能需求`：角色/权限配置、历史数据处理、性能、可靠性、数据安全、风控/合规、运营实施、功能效果评估（数据埋点）。
7. `六、UI信息`：UI老师、UI链接、UI截图；缺失时写“待补充”。
8. `七、会议信息`：类型、会议时间/人员、会议结论；未知内容写“待补充”。
9. 视复杂度增加：待确认事项、验收标准、接口/数据模型、流程图或原型说明。

## 需求描述写法

- 使用中文产品术语，首次出现的英文保留英文并给出中文含义。
- 用“位置：”“入口：”“交互逻辑：”“数据逻辑：”“异常处理：”“状态：”等标签组织内容。
- 交互必须写清触发控件、前置条件、页面反馈、成功结果、失败结果、返回行为和重复操作处理。
- 数据逻辑必须写清数据主体、来源、写入/读取时机、计数口径、幂等、并发、失败回滚和历史数据影响。
- 状态至少覆盖默认、加载中、成功、失败、无权限/未开通、额度用完（如适用）。
- 每项功能尽量给出可验证的文案和 toast；不确定文案标记“待确认”。
- 不把普通智能推题和 AI 分层推题混为一套规则；需要明确两者入口、题目结构、次数、结果和异常差异。
- 灰度功能必须说明灰度主体、开关位置、生效范围、关闭后的行为和历史数据处理。

## 表格规范

严格保留用户要求的表头和顺序。常用表格如下：

- 业务背景：`需求方 | 产品关联/链接 | 业务问题 | 使用场景 | 业务诉求 | 上线排期 | 价值收益`（若原模板是两列表，按原模板输出）。
- 功能清单：`ID | 功能 | 覆盖范围 | 优先级`。
- 能力集成：`集成能力 | 主要作用 | 产出`。
- 页面方案：`系统/模块 | 产品原型 | 需求描述`。
- 数据字段：`字段 | 说明`。
- 额度流水：`action | 发生时机 | 是否影响正式已用次数`。

### 埋点表格（必须遵循）

当用户要求埋点时，使用截图中的四列表格，不改成其他形式：

`事件名 | 关键字段 | 触发时机 | 用途`

前端与后端分开写成 `5.1 前端行为埋点`、`5.2 后端结果埋点`（章节编号按所在文档调整）。关键字段放在单元格内，格式为“业务说明<br>关键字段：field_a、field_b”。

- 前端埋点记录点击/曝光等行为，用于行为分析，不作为额度扣减依据。
- 后端埋点记录实际成功结果，用于统计学校使用量、换题量和下载量，并作为额度扣减或业务统计依据。
- 若用户限定统计范围，只保留用户指定指标，不扩展教师路径、模型成本、失败原因等指标。
- 事件命名使用稳定的英文 snake_case；关键主体优先使用 `school_id`，按需补充 `task_id`、题目 ID、时间和状态。

## 额度与后台需求写法（适用时）

对于按学校购买、按周期赠送次数的 AI 功能，必须说明：

1. 额度主体为学校，教师共享；周期、总额度、已用、预占、剩余和人工调整记录。
2. 成功任务才正式扣次；失败/超时释放预占；重复提交和回调幂等；并发最后一次额度不能超扣。
3. 额度用完时的前端展示、禁用交互、替代方案和服务端兜底。
4. 后台是否需要灰度开关、服务周期、额度查询、人工调额、流水查询和导出。没有后台范围时写明“建议作为 P0/P1，并标注待确认”。

## 输出前检查清单

- 是否严格采用用户提供的章节顺序、表头和编号格式？
- 每个功能是否同时写了位置、交互逻辑和数据逻辑？
- 正常、加载、成功、失败、无权限/额度用完状态是否完整？
- 普通方案与 AI 方案的差异是否明确？
- 数据统计是否定义了成功口径、重复计数、失败回滚和学校维度？
- 前端埋点和后端埋点是否分开，且埋点表是否为“事件名 | 关键字段 | 触发时机 | 用途”？
- 未知信息是否标注“待确认/待补充”，没有臆造？
- 是否给出可执行的验收标准？

## 参考资料

需要查看内置章节模板、AI 分层推题示例或埋点样例时，读取 [references/prd-template.md](references/prd-template.md)。除非用户提供新版模板，否则不要要求用户再次上传模板。
